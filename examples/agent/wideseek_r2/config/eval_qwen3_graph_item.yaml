defaults:
  - base_eval
  - _self_

runner:
  experiment_name: graph_item
  output_dir: ./logs/eval

agentloop:
  workflow: mas_graph
  toolcall_parser: wideseek_r2-graph-qwen
  graph_memory:
    enabled: true

data:
  type: wideseek_r2
  answer_type: item
  is_gisa: true
